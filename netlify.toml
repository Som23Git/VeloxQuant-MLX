[build]
  base    = "/"
  command = """
    mkdir -p dist/docs &&
    cp -r landing/* dist/ &&
    cd docs-site && npm install && npm run build && cd .. &&
    cp -r docs-site/build/* dist/docs/
  """
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
