[build]
  base    = "/"
  command = """
    mkdir -p dist/landing &&
    cp -r landing/* dist/landing/ &&
    cd docs-site && npm install && npm run build && cd .. &&
    cp -r docs-site/build/* dist/
  """
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
