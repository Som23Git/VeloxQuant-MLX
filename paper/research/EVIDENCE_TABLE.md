# Phase 1 — Claims-to-Evidence Map (VeloxQuant-MLX)

Forensic audit of every headline number before any of it enters the paper.
Confidence: **verified** (traced to code + measured data file I read), **plausible**
(derivable from measured data but not stated as a single measured row), **unverified**
(asserted in README/blog with no data artifact I could locate), **contradicted**
(measured data disagrees with the marketing claim).

| # | Claim (as stated) | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 1 | **16× key compression** (VecInfer-1bit) | README "Numbers that matter" | `quantizers/turboquant_prod.py`, `cache/vecinfer_cache.py`, `metal/_vecinfer.py` | `figures/vecinfer/Qwen2.5-7B-Instruct-4bit/results.json` → `VecInfer-1bit key_compression=16.0` | Qwen2.5-7B-4bit, head_dim=128, n_kv_heads=4, S≈120 | **verified** | This is *key-only* compression, not full-KV. Must be labeled as such. |
| 2 | **VecInfer-1bit exceeds fp16 throughput at 16×** | README, BLOG | same | same json: VecInfer-1bit 21.5 tok/s vs fp16 21.0 tok/s, 121 vs 120 tokens | Qwen2.5-7B-4bit, M-series, S≈120 | **verified** | True for *this one model*; attributable to strong GQA (4 kv / 28 q heads). Do not generalize. |
| 3 | **13× Metal kernel speedup** (`quantize_vq`) | README, CHANGELOG 0.5.1 | `metal/_vecinfer.py` `vecinfer_quantize_metal` | `figures/metal/results.json` → S=2048,D=128: speedup=13.17; up to 14.68 at S=8192 | head_dim=128, n_centroids=256, sub_dim=8 | **verified** | Range is 6.9×(S=128) → 14.7×(S=8192). "13×" = the S=2048 point. State the range. |
| 4 | **98% peak-memory reduction** | README, CHANGELOG 0.5.1 | `metal/_vecinfer.py` (register-accumulated argmin, no materialized diff tensor) | `figures/metal/results.json` → memory: pure 729.26 MB → metal 12.00 MB, reduction 98.35% | H=4,S=4096,D=256,sub_dim=4,n_centroids=256 (the OOM-trigger shape) | **verified** | Single shape. Genuine and the most defensible systems result. |
| 5 | **dequant kernel speedup** | implied by "Metal kernels make hot path faster" | `metal/_vecinfer.py` `vecinfer_dequant_metal` | `figures/metal/results.json` dequantize block: speedups 0.75–1.16× | various | **contradicted (partially)** | Dequant kernel is **at or below MLX parity** (often slower). CHANGELOG is honest about this; README's blanket "13× faster hot path" overstates. Paper must scope the 13× to *quantize only*. |
| 6 | **RVQ-1bit 7.5× compression, near-zero throughput cost** | README | `quantizers/turboquant_rvq.py`, `cache/turboquant_rvq_cache.py` | Qwen2.5-7B json: RVQ-1bit key_compression=7.53, 20.7 tok/s vs 21.0 fp16 (98.5%) | Qwen2.5-7B-4bit | **verified** | 7.53× and ~98% throughput retained. "Near-zero cost" fair. But 112/120 tokens (mild early-stop). |
| 7 | **RaBitQ 6× full-KV compression** (1-bit K + MSE-b4 V) | README, BLOG, CHANGELOG 0.7.0 | `quantizers/rabitq.py`, `metal/_rabitq.py`, `quantizers/turboquant_mse.py` | `figures/RaBitQ/falcon/results.json` → `ratio_rbmse=5.953` (const across S) | Falcon3-7B, D=256, key_ratio=11.64, val 4×(MSE-b4) | **verified** | 5.95× ≈ "6×". This is the headline RaBitQ number and it is real. |
| 8 | **RaBitQ 103k tokens @ 8 GB** (vs 17k fp16) | README, BLOG | same | `figures/RaBitQ/falcon/results.json` long_context: rb_mse4v 0.616 GB @ 32768 tok → linear extrapolation 8 GB ≈ 106k tok; fp16 3.67 GB @ 32768 → 8 GB ≈ 71k... | Falcon3-7B KV-only memory model | **plausible (extrapolated)** | 103k and 17k are **extrapolations**, not measured rows. The 17k fp16 figure implies a *total-RAM* budget (weights+OS+KV), not the KV-only table. Label as model-based estimate, give the assumption, do not present as measured. |
| 9 | **RaBitQ throughput / search quality** | (omitted from README headline) | `metal/_rabitq.py` `rabitq_hamming_score` | `figures/RaBitQ/falcon/results.json` throughput; `figures/RaBitQ/kernel/results.json` recall_at_10 | Falcon3-7B; kernel D=128,nlist=64,nprobe=8,rerank=32 | **contradicted / negative result** | rb_mse4v is **~50–60 ms/step vs fp16 ≈0 ms** (fp16 baseline reads 0.000 ms → not actually timed; speedup framing invalid). ANN **recall@10 = 0.0–0.4** — search path is not usable for retrieval. Paper must report this as a limitation, not bury it. |
| 10 | **SpectralQuant +3pp / +7.4pp / +10.4pp cosim over TurboQuant** | README | `spectral/spectral_quant.py`, `spectral/participation_ratio.py`, `cache/spectral_cache.py` | README table (Qwen2.5-0.5B 0.9072 vs 0.8329; Gemma4-4B 0.8625 vs 0.7581); figures under `figures/spectral_quant_*/` (PNGs, no json located) | Qwen2.5-0.5B, Gemma-4-4B, same bit-width | **plausible** | Cosine deltas asserted with figures but **no results.json** found to read raw numbers. Tests (`test_spectral_quant.py`, `test_ablation.py`) pass and confirm the rotation improves cosine, but exact pp deltas unverified at source. Cite as measured-in-repo, flag missing json. |
| 11 | **SpectralQuant 5.95× compression vs TurboQuant 5.02× same bit-width** | README | `spectral/spectral_quant.py` | README table (SQ ratio 5.33×) | Qwen2.5-0.5B / Gemma4 | **plausible** | README headline says 5.95×; per-model table says 5.33×. Internal inconsistency → use the table value, flag. |
| 12 | **RateQuant 2.7× lower PPL degradation at 2.0 avg bits vs uniform 2-bit** | README/BLOG | `allocators` (`allocate_bits_ratequant`, `calibrate_layer_sensitivities`), tests `test_ratequant.py` | README RateQuant table; `figures/outlier_ratequant/` | Falcon3-7B / Gemma3-4B | **plausible** | Mechanism (reverse-waterfilling, Thm 2 closed form) verified in code + tests. The 2.7× PPL figure not traced to a json I read. Allocation tables (6.48× sensitivity → 14×b2/14×b1) are verified against README; PPL multiplier flagged. |
| 13 | **RateQuant retains 100% fp16 (Falcon3) / 91% (Gemma3) at 5.22× compression** | README | same | README throughput table (Falcon3 22.8/22.9=100%; Gemma3 36.3/39.8=91%) | Falcon3-7B, Gemma3-4B | **plausible** | Consistent across two README tables; no contradicting data. Treat as measured-in-repo. |
| 14 | **CommVQ 64× key compression** (RoPE-commutative VQ) | README, CHANGELOG | `quantizers/comm_vq.py`, `metal/_comm_vq.py`, tests `test_comm_vq.py` | README; tests verify commuting-form projection + reconstruction | D=128, n_cb=4 | **plausible** | 64× is a *theoretical* ratio from codebook config (D=128, 4 codebooks → bits/key tiny). Tests confirm correctness of the RoPE-commutative constraint; the 64× number is an analytic storage ratio, not an end-to-end quality-validated result. Label analytic. |
| 15 | **243 tests passing** | README badge | `veloxquant_mlx/tests/` | `grep def test_` = 231; CHANGELOG 0.5.1 says "212"; `pytest --collect-only` = **314** (parametrized) | repo HEAD | **contradicted** | Three different numbers (231 fns / 243 badge / 314 collected). Use the reproducible figure: **314 collected, subset run passes**. Do not cite "243". |
| 16 | **12 production models validated** | README | `benchmark_scripts/benchmark_*.py` | `figures/vecinfer/` has 12 model dirs (+ `_summary`): DeepSeek-V2-Lite, Falcon3-7B, Llama3.1-8B, Llama3.2-1B/3B, Mistral-7B, Phi-4, Qwen2.5-7B, Qwen3-4B/8B, SmolLM2-135M, Gemma3-4B | M-series, S≈120, max_tokens 120 | **verified** | 12 model result dirs exist. "Validated" = VecInfer/TQ/RVQ sweep ran; **not** downstream-task validation. |
| 17 | **Qwen2.5-32B OOM negative result** | BLOG, CHANGELOG | `benchmark_scripts/benchmark_qwen25_32b.py` | BLOG watchdog log; `figures/qwen25_32b/` | 16 GB M4, weights ~17.5 GB | **plausible** | Honest negative result. Log quoted in BLOG; treat as reported-by-authors. Good for Limitations. |
| 18 | **TurboQuant distortion within theoretical bound √(3π)/2·4^(−b)** | implied | `quantizers/turboquant_mse.py`, `turboquant_prod.py` | `tests/integration/test_distortion_bounds.py` (parametrized b=1..4, PASSES) | synthetic unit-norm Gaussian, d=128 | **verified** | Strongest theoretical-fidelity evidence in the repo. Scalar Lloyd-Max overshoots optimum by ≤2.5× (documented in test). |
| 19 | **3-bit repetition-loop / 4-bit near-lossless on Llama3.2-3B** | BENCHMARK_RESULTS.md | `cache/turboquant_cache.py` | `benchmark_scripts/benchmark_kv.py`; BENCHMARK_RESULTS.md | Llama3.2-3B-4bit, M4, S=200 | **verified (qualitative)** | cosim ~0.85 (3b) vs ~0.95 (4b) reported; output samples included. Qualitative quality claim, single model. |
| 20 | **Optimization journey 1.2–2.6× throughput, zero quality regression** | OPTIMIZATION_FINDINGS.md | `benchmark_core.py`, `codebooks/scalar_codebook.py` | OPTIMIZATION_FINDINGS.md stage table; `figures/updated_tests/optimization_journey.png` | Mistral-7B, Qwen3-4B | **plausible** | Stage-by-stage table + bitwise boundary-sum parity (100% index match) is rigorous. Two models only. |

## KIVI additions (v0.8.0 — implemented + measured this session)
| # | Claim (as stated) | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 21 | **KIVI-2bit ≈ 5.8× key compression** | README, CHANGELOG 0.8.0 | `quantizers/kivi.py`, `cache/kivi_cache.py` | `figures/kivi/{Llama-3.2-3B,Qwen2.5-7B,Mistral-7B}/results.json` → key_compression 5.79 / 5.78 / 5.76 | Apple M4, b=2, group_size=32, residual_length=32, long-context prompt, max_tokens≈120 | **verified** | Realized accounting (codes+params+residual). Consistent across 3 models. |
| 22 | **KIVI-2bit ≈ 4× full-KV compression** | README, CHANGELOG | same | same json → full_kv_compression 3.98 / 3.98 / 4.03 | same | **verified** | Includes fp16 residual window — not inflated. |
| 23 | **KIVI-2bit retains 100–106% fp16 throughput** | README, CHANGELOG | same | same json → KIVI-2bit vs fp16-baseline throughput_tok_s (16.3/16.0, 7.6/7.6, 6.8/6.4) | same | **verified** | fp16 baseline is **really timed** (non-zero), unlike the RaBitQ jsons. |
| 24 | **KIVI-2bit raw-key cosine ~0.93 (lossy)** | README, docs | `quantizers/kivi.py` | `tests/quantizers/test_kivi.py::test_channel_reconstruction_cosine`; `scripts/plot_kivi.py` fig1 | synthetic unit-norm Gaussian, d=128, b=2, group_size=32 | **verified** | Deterministic; this is *why* the fp16 residual exists. Honestly reported as lossy. |
| 25 | **KIVI peak runtime memory not reduced** | README, docs | `cache/kivi_cache.py` (dequant-to-fp16 before SDPA) | `figures/kivi/*/results.json` peak_mb (KIVI ≈ or slightly > fp16) | same | **verified (negative)** | Compression is storage-accounting, not peak working set. Reported plainly. |
| 26 | **KIVI compression = 1.0× at short prompts** | README, docs | `cache/kivi_cache.py` residual-window logic | observed during benchmark dev (short prompt → whole prefill in residual) | prompt_tokens ≤ residual_length | **verified (negative)** | Correct algorithm behavior; benchmark uses a long prompt to exercise the quantized path. |
| 27 | **VecInfer-2bit compresses harder than KIVI-2bit (8× vs 5.8× keys)** | README, docs, fig4 | `scripts/plot_kivi.py` `fig4_vs_existing` | `figures/kivi/fig4_vs_existing.png` from kivi + vecinfer jsons | same 3 models | **verified** | Honest framing: KIVI is the baseline, not the winner. |
| 28 | **+25 KIVI tests; 334/339 full-suite pass** | CHANGELOG | `tests/quantizers/test_kivi.py`, `tests/cache/test_kivi_cache.py` | `pytest` full run this session | repo HEAD + KIVI | **verified** | The 5 failures are the pre-existing flaky VecInfer parity tests (rows above), not KIVI. |

## Hardware / experimental setup (as found)
- **Chip:** Apple M4, 16 GB unified memory (BENCHMARK_RESULTS.md, OPTIMIZATION_FINDINGS.md). Some figures predate and may be M-series generic — not all artifacts state the chip.
- **Framework:** MLX ≥ 0.18, Python 3.11/3.12.
- **Models (12):** DeepSeek-V2-Lite, Falcon3-7B, Llama-3.1-8B, Llama-3.2-1B/3B, Mistral-7B-v0.3, Phi-4, Qwen2.5-7B, Qwen3-4B/8B, SmolLM2-135M, Gemma-3-4B (all 4-bit weight-quantized mlx-community builds), plus Qwen2.5-32B (OOM).
- **Metrics:** key compression ratio, throughput (tok/s), peak memory (MB), cosine similarity, SNR (dB), MSE/IP distortion vs theoretical bound, ANN recall@10 (RaBitQ only), tokens-generated (a coherence proxy).
- **Sequence lengths:** generation benchmarks S≈120–200; Metal microbenchmarks S up to 8192; RaBitQ memory model up to 32768.

## Gaps that must be disclosed as Limitations
1. **No downstream-task accuracy** anywhere — no LongBench, no WikiText perplexity sweep across configs (only a single fp16 PPL=20.16 for Falcon3 in RaBitQ json), no QA/needle-in-haystack. "Quality" = cosine similarity + a tokens-generated coherence proxy. This is the single biggest barrier to a main-conference accept.
2. **RaBitQ search path is non-functional for retrieval** (recall@10 ≤ 0.4) and slow; only its *memory-compression* story holds.
3. **fp16 throughput baselines read 0.000 ms** in RaBitQ jsons — those speedup comparisons are measurement artifacts.
4. **Single-chip (M4), small-S generation** — throughput conclusions may not transfer across M-series tiers or long context.
5. **Most algorithms are re-implementations** of published methods (see novelty ledger below).

## Novelty ledger (honest delta)
| Component | Upstream origin | VeloxQuant delta |
|---|---|---|
| TurboQuant (rotation + Lloyd-Max + QJL residual) | Zandieh et al., ICLR 2026 | MLX re-impl; `mx.fast.hadamard_transform` O(d log d) rotation |
| QJL (1-bit JL sketch) | Zandieh et al., AAAI 2025 | MLX + Metal `_qjl` kernel |
| VecInfer (smooth + Hadamard dual transform, product VQ) | Yao et al., arXiv:2510.06175 | **Metal quantize kernel (verified 13× / 98% mem)** — the genuine systems contribution |
| RaBitQ (randomized rotation + sign packing + IVF) | SIGMOD 2024; Ascend-RaBitQ arXiv:2605.16007 | MLX re-impl + Metal Hamming kernel; memory story works, search does not |
| CommVQ (RoPE-commutative additive VQ) | Apple/UMass arXiv:2506.18879 | MLX re-impl with EM commuting-form projection |
| RateQuant (reverse-waterfilling bit allocation) | arXiv:2605.06675 | MLX allocator + sensitivity probe; per-layer only |
| PolarQuant | AISTATS 2026 | MLX re-impl |
| SpectralQuant (SVD rotation, signal/noise split) | (author-original framing on top of spectral KV findings) | **Most plausibly novel algorithmic piece** |
| RVQ analytical Gaussian/Laplacian codebooks | classical RVQ + TurboQuant | zero-calibration analytical codebook precompute |
| PALU (group-head low-rank K+V projection) | Chang et al., ICLR 2025, arXiv:2407.21118 | MLX re-impl; **true latent storage** (cache holds `[S,r]`, not full fp16 — unlike repo's SVDq); group-head SVD fit from prefill batch; mixed-bit latents reuse SVDq coder; fused recon-attention kernel **not** ported |
| CacheGen (entropy-coded KV via token locality) | Liu et al., SIGCOMM 2024, arXiv:2310.07240 | MLX re-impl; **first entropy-coding method in suite**; token-delta + Shannon-entropy byte model (no serial range codec — modelled + capped at fixed-width); reconstruction identical to group quant |
| MiniCache (cross-layer depth SLERP merge) | Liu et al., NeurIPS 2024, arXiv:2405.14366 | MLX re-impl; **merges tensors** via SLERP (vs XQuant's code reuse); shared coordinator integration; magnitude/direction split + token retention; fp16 directions (no extra quant) |

**Genuine contributions:** (a) first unified, Metal-accelerated KV-cache quantization *suite* for Apple-Silicon unified memory; (b) a verified Metal `quantize_vq` kernel (13× / 98% peak-mem); (c) a clean 3-line `mlx_lm` integration; (d) a broad 12-model engineering sweep; (e) SpectralQuant as the most novel algorithm. **Not** invention of the underlying quantizers.

## KVSink-adapted sink protection (0.9.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 21 | Planted high-norm sinks detected & preserved bit-exact; neighbors quantized | README 0.9.0 section, CHANGELOG | `cache/sink_cache.py` | `tests/cache/test_sink_cache.py::test_planted_sinks_detected_and_preserved` (PASSES) | synthetic, 25× planted sinks at {0,7,20}, S=64, b=2 | **verified** | Unit-test level only. |
| 22 | Sink-protected MSE < plain KIVI at equal bit-width | README, CHANGELOG | same | `::test_sink_protection_beats_plain_kivi_on_planted_sinks` (PASSES) | synthetic planted sinks {0,7,20,41,90}, S=128, b=2 | **verified (synthetic)** | Requires calibration exclusion; without it the claim FAILS (reproduced during development — sinks inflate group scales). |
| 23 | Dynamic selection MSE < Preserve-First-N at equal fp16 budget | README, CHANGELOG | same | `::test_dynamic_selection_vs_pfn_equal_budget` (PASSES) | sinks NOT all at front ({0,7,20,41,90}), k=5 | **verified (synthetic)** | The KVSink paper's central claim, reproduced at cache level. |
| 24 | `n_sink_tokens=0` ≡ plain KIVI bit-for-bit | CHANGELOG | same | `::test_zero_sinks_equals_plain_kivi` (PASSES) | S=64, b=2 | **verified** | |
| 25 | "KVSink-adapted", not faithful port | all docs | `sink_cache.py` docstring | design decision: paper needs hidden-state emergence-layer signal, cache sees only K/V; proxy = key L2-norm | — | **verified (by construction)** | Labeling rule: never plain "KVSink". |
| 26 | End-to-end throughput/compression for kivi_sink | (none claimed) | `benchmark_scripts/benchmark_sink.py` | **NOT RUN** — no results.json exists | — | **unverified — explicitly not claimed** | Benchmark skipped per user; docs state this. |
| 27 | Full suite 344/348 passing | README badge, CHANGELOG 0.9.0 | — | `pytest -q` run 2026-06-12: 344 passed, 4 failed (pre-existing flaky VecInfer parity set) | repo HEAD post-0.9.0 | **verified** | Flake count varies 4–5 run to run; same documented set. |

## PALU true-latent low-rank K+V (0.15.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 28 | Cache stores latent `[S, r]`, not full fp16 `[S, D]` (parent ring buffer bypassed) | README, docs, CHANGELOG 0.15.0 | `cache/palu_cache.py` | `tests/cache/test_palu_cache.py::test_storage_is_latent_not_full_fp16` (PASSES) | synthetic, D=64, r=16, H=4 | **verified (synthetic)** | The structural differentiator vs SVDq. Asserts `cache.keys is None` and latent buffer last-dim == r. |
| 29 | PALU reconstruction MSE < naive 2-bit on **both** K and V (low-rank data) | README, docs | `quantizers/palu.py`, `cache/palu_cache.py` | `::test_reconstruction_lower_mse_than_raw_2bit_both_tensors` (PASSES); offline harness in `benchmark_palu.py` (K 1.54 vs 2.37, V 1.54 vs 2.52 at S=256,D=128,r=16) | synthetic rank-8 (test) / rank-16 (harness) | **verified (synthetic)** | Both tensors, unlike SVDq which leaves values fp16. Offline harness numbers are synthetic, not model-level. |
| 30 | Full-KV effective rate < 1 bit/element at default settings | README, docs | `cache/palu_cache.py` `assigned_avg_bits` | `::test_assigned_avg_bits_sub_2` (PASSES); offline harness reports 0.31 bits at r=16/D=128 | r/D × mixed-bit weighting | **verified (analytic)** | Analytic rate `(r/D)·avg_bits`; exact value depends on rank chosen at prefill. |
| 31 | Both K and V compressed vs fp16 (byte accounting) | README, docs | `cache/palu_cache.py` `_account_bytes` | `::test_both_tensors_compressed`, `::test_low_rank_only_values_still_compress` (PASS) | realised latent + projection bytes | **verified (synthetic)** | Accounting includes amortised V_g/μ_g projection bytes; not inflated. |
| 32 | "PALU-adapted", not faithful port | all docs | `palu_cache.py` / `palu.py` docstrings | design decisions: prefill-batch SVD (not offline calib); uniform rank across groups; fused recon-attention kernel not ported | — | **verified (by construction)** | Labeling rule: never plain "PALU". Peak memory at attend time **not** reduced (fp16 reconstructed for SDPA) — stated as limitation. |
| 33 | End-to-end throughput/compression for palu | (none claimed) | `benchmark_scripts/benchmark_palu.py` | **NOT RUN** — no results.json exists | — | **unverified — explicitly not claimed** | Same discipline as kivi_sink row 26: no model-level numbers until json committed. |
| 34 | `for_model()` now propagates method-specific config fields | CHANGELOG 0.15.0 | `cache/base.py` (dataclasses.replace) | verified manually during dev: `palu_rank=24` survives `for_model`; previously dropped to default | fake 3-layer model | **verified** | Pre-existing bug fix; also repairs svdq_*/kitty_*/kvquant_* propagation through `for_model`. |

## CacheGen entropy coding + MiniCache depth merge (0.16.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 35 | CacheGen reconstruction is identical to plain group quant (lossless over codes) | README, docs, CHANGELOG 0.16.0 | `cache/cachegen_cache.py`, `quantizers/cachegen.py` | `tests/cache/test_cachegen_cache.py::test_reconstruction_matches_group_quant`; `tests/quantizers/test_cachegen.py::test_drop_in_matches_group_quant` (PASS) | synthetic, b=4, gs=16/32 | **verified (synthetic)** | The entropy layer is storage-only; values byte-identical to `_group_quant_dequant`. |
| 36 | Token-delta entropy < raw entropy on correlated data | docs | `quantizers/cachegen.py` `token_delta`, `symbol_entropy_bits` | `::test_delta_lowers_entropy_on_correlated_data`, `::test_entropy_bytes_smaller_on_correlated` (PASS) | random-walk codes, S=128, D=64 | **verified (synthetic)** | The locality CacheGen exploits. Offline harness: ~17% savings on correlated 3-bit. |
| 37 | Entropy-coded bytes never exceed fixed-width packing (cap) | README, docs | `quantizers/cachegen.py` `entropy_coded_bytes` | `::test_entropy_bytes_capped_at_fixed_width`, `::test_savings_never_negative_on_random_data` (PASS); offline harness iid savings = 0.0 | iid Gaussian codes | **verified (synthetic)** | Models a real coder's raw-fallback; prevents "compression made it bigger". |
| 38 | Entropy-coded byte size is modelled, not coded by a shipped range codec | all docs | `quantizers/cachegen.py` docstring | design decision: Shannon-entropy estimate of delta stream | — | **verified (by construction)** | Labeling rule: never plain "CacheGen". No serial codec (would bottleneck MLX decode). Storage-only win, not bandwidth. |
| 39 | CacheGen end-to-end throughput/compression | (none claimed) | `benchmark_scripts/benchmark_cachegen.py` | **NOT RUN** — no results.json | — | **unverified — explicitly not claimed** | Same discipline as prior methods. |
| 40 | MiniCache: similar layers merge with low MSE; dissimilar 100% retained | README, docs, CHANGELOG 0.16.0 | `quantizers/minicache.py`, `cache/minicache_cache.py` | `tests/cache/test_minicache_cache.py::test_merge_layer_reconstructs_similar_primary`, `::test_dissimilar_tokens_retained` (PASS); offline harness similar cos 0.9995→MSE 2e-4, dissimilar cos −0.01→100% retained MSE 0 | synthetic similar (+0.02–0.03 noise) vs iid | **verified (synthetic)** | The merge-vs-retain decision works as the paper describes. |
| 41 | SLERP output is unit-norm; endpoints return inputs; magnitude preserved | docs | `quantizers/minicache.py` `slerp`, `reconstruct_layer` | `tests/quantizers/test_minicache.py::test_slerp_*`, `::test_merge_preserves_magnitude` (PASS) | synthetic; 3× magnitude ratio survives shared direction | **verified (synthetic)** | Magnitude/direction decomposition is exact. |
| 42 | Cross-layer merge via shared coordinator; roles assigned middle-to-deep only | README, docs | `cache/minicache_coordinator.py`, `base.py` `_build_minicache`, `quantizers/minicache.py` `pair_layers_depth` | `::test_for_model_assigns_primary_and_merge`, `::test_early_layers_never_merged`, `::test_coordinator_max_ctx_guard` (PASS) | fake 8-layer model, start_frac=0.5 | **verified (synthetic)** | Same integration pattern as XQuant; merges *tensors* (XQuant reuses *codes*). |
| 43 | MiniCache "adapted", not faithful port; storage-only win | all docs | `minicache_cache.py` docstring | design decision: coordinator integration not modified attention forward; fp16 directions; reconstruct fp16 for SDPA | — | **verified (by construction)** | Labeling rule: never plain "MiniCache". Peak memory at attend time not reduced. |
| 44 | MiniCache end-to-end throughput/compression | (none claimed) | `benchmark_scripts/benchmark_minicache.py` | **NOT RUN** — no results.json | — | **unverified — explicitly not claimed** | |

## GEAR error-feedback (residual low-rank + sparse outliers) (0.17.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 45 | GEAR reconstruction MSE strictly below base-quant-alone on low-rank+outlier data | README, docs, CHANGELOG 0.17.0 | `quantizers/gear.py`, `cache/gear_cache.py` | `tests/quantizers/test_gear.py::test_gear_beats_base_quant`; `tests/cache/test_gear_cache.py::test_beats_naive_base_reconstruction` (PASS) | synthetic low-rank (r=6) + sparse outliers, b=2 | **verified (synthetic)** | The core error-feedback claim. Unlike CacheGen, reconstruction is genuinely improved, not identical to group quant. |
| 46 | Low-rank-alone and sparse-alone each help; rank=0,sparse=0 collapses to base quant | docs | `quantizers/gear.py` `lowrank_error`, `sparse_outliers` | `::test_lowrank_alone_helps`, `::test_sparse_alone_helps`, `::test_base_only_equals_group_quant` (PASS) | synthetic, b=2 | **verified (synthetic)** | The two feedback terms are independently useful and composable over the borrowed base quant. |
| 47 | Residual low-rank recovers a known rank-r error to < eps; sparse picks true outliers | docs | `quantizers/gear.py` (`_quant_utils._truncated_svd`) | `::test_residual_lowrank_recovers_known_rank`, `::test_sparse_selects_true_outliers` (PASS) | exact rank-r residual; planted top-magnitude entries | **verified (synthetic)** | Truncated-SVD helper shared with SVDq/PALU, applied to the error not the signal. |
| 48 | Byte accounting ordered base_only ≤ compressed ≤ fp16 at realistic head dim, low rank | README, docs | `quantizers/gear.py` `gear_bytes`, `base_only_bytes`; `cache/gear_cache.py` | `::test_byte_accounting_ordering` (quantizer + cache) (PASS) | D=128, rank=8, sparse 0.5% | **verified (synthetic)** | Overhead caveat documented: rank must be low vs D or factors exceed fp16; reported honestly. |
| 49 | error_recovery_ratio in (0,1]; values-off keeps values fp16; build via both paths | docs | `cache/gear_cache.py` | `::test_error_recovery_positive`, `::test_values_off_keeps_values_fp16`, `::test_build_via_for_model_propagates_config` (PASS) | synthetic; fake 2-layer model | **verified (synthetic)** | `for_model` propagates `gear_*` via `dataclasses.replace` (no builder branch needed). |
| 50 | GEAR "adapted", not faithful port; storage-only stored win | all docs | `gear_cache.py` / `gear.py` docstrings | design decision: per-call residual SVD (not offline calib); fused dequant kernel not ported; reconstruct fp16 for SDPA | — | **verified (by construction)** | Labeling rule: never plain "GEAR". Attend-time peak memory not reduced (fp16 reconstructed); low-rank/sparse factors are overhead. |
| 51 | GEAR end-to-end throughput/compression | (none claimed) | `benchmark_scripts/benchmark_gear.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — no results_gear.json committed | — | **unverified — explicitly not claimed** | Offline harness reports 11–22% MSE improvement on synthetic data; not model-level. |

## ZipCache-adapted per-token mixed bit-width (0.18.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 52 | Saliency mask selects exactly ceil(S×hi_fraction) tokens by key L2-norm | docs, CHANGELOG 0.18.0 | `quantizers/zipcache.py` `saliency_mask`, `token_key_norms` | `tests/quantizers/test_zipcache.py::test_saliency_mask_selects_top_fraction`, `::test_saliency_mask_selects_highest_norms` (PASS) | S=20, hi_fraction=0.25 | **verified (synthetic)** | Exact count (ceil). Selected tokens are the highest-norm ones — validated with monotone norms (0..9). |
| 53 | 4-bit channel quant cosine > 0.995; 2-bit cosine > 0.8 | docs | `quantizers/zipcache.py` `channel_quant`, `channel_dequant` | `::test_channel_quant_4bit_near_lossless`, `::test_channel_quant_2bit_lossy_bounded` (PASS) | synthetic Gaussian N(0,1), S=64, D=128 | **verified (synthetic)** | 4-bit is high-fidelity; 2-bit is lossy but bounded. Group-size=32 token groups. |
| 54 | hi_fraction=1 (all hi_bits) has lower MSE than hi_fraction=0 (all lo_bits) | docs | `quantizers/zipcache.py` | `::test_uniform_hi_bits_roundtrip` (PASS) | synthetic Gaussian, 4-bit vs 2-bit | **verified (synthetic)** | Core ordering claim: more bits → less error. |
| 55 | Mixed-bit stored bytes < fp16; mixed-bit bytes ≥ all-lo-bit baseline | README, docs | `quantizers/zipcache.py` `zipcache_bytes`, `base_only_bytes` | `::test_byte_ordering_compressed_lt_fp16`, `::test_byte_ordering_between_lo_and_fp16` (PASS) | S=D=128, hi_fraction=0.2 | **verified (synthetic)** | Hi-bit overhead exceeds all-lo; full-rate still below fp16. Both bounds are honest. |
| 56 | effective_avg_bits in [lo_bits, hi_bits]; compression_ratio > 1 | README, docs | `cache/zipcache_cache.py` | `tests/cache/test_zipcache_cache.py::test_effective_avg_bits_in_range`, `::test_byte_ordering_compressed_lt_fp16` (PASS) | default config (hi=4, lo=2, frac=0.2) | **verified (synthetic)** | Mixed-bit rate is exactly in (lo_bits, hi_bits) as expected. |
| 57 | Values-off passthrough lossless; compressed_value_bytes=0 | docs | `cache/zipcache_cache.py` | `::test_values_off_passthrough` (PASS) | zipcache_quantize_values=False | **verified (synthetic)** | fp16 values accumulate unmodified. |
| 58 | for_model propagates zipcache_* fields via dataclasses.replace | docs | `cache/base.py` `KVCacheBuilder.for_model` | `::test_build_via_for_model_propagates_config` (PASS) | fake 2-layer model | **verified (synthetic)** | No builder branch needed; single-layer (no coordinator). |
| 59 | ZipCache "adapted", not faithful port; proxy weakness disclosed | all docs | `zipcache_cache.py` / `zipcache.py` docstrings | design decision: key-norm proxy, not attention scores; both groups quantized (not fp16) | — | **verified (by construction)** | Labeling rule: "ZipCache-adapted". Third use of key-norm proxy. Proxy weakness stated in docs, CHANGELOG, README. |
| 60 | ZipCache end-to-end throughput/compression | (none claimed) | `benchmark_scripts/benchmark_zipcache.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — no results_zipcache.json committed | — | **unverified — explicitly not claimed** | |

## SnapKV-adapted prefill eviction (0.19.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 61 | obs_window_attention_scores output shape [S], dtype fp32, values ∈ [0, 1] | docs | `quantizers/snapkv.py` `obs_window_attention_scores` | `tests/quantizers/test_snapkv.py::test_obs_window_scores_shape`, `::test_obs_window_scores_dtype_fp32`, `::test_obs_window_scores_in_range` (PASS) | S=64, D=128, obs_window=8 | **verified (synthetic)** | Scores are mean of softmax rows; necessarily in [0,1]. |
| 62 | obs_window clamped to S — no index error when obs_window > S | docs | `quantizers/snapkv.py` `obs_window_attention_scores` | `::test_obs_window_clamp_large_window` (PASS) | S=16, obs_window=999 | **verified (synthetic)** | min(max(obs_window,1), S) clamp at line ~72. |
| 63 | snap_select_indices returns exactly min(budget, S) sorted-ascending indices | docs | `quantizers/snapkv.py` `snap_select_indices` | `::test_select_count_exact`, `::test_select_sorted_ascending` (PASS) | budget=3, S=6 | **verified (synthetic)** | Sorted union of sink_set and top_dynamic set. |
| 64 | First n_sink positions always in kept set | docs | `quantizers/snapkv.py` `snap_select_indices` | `::test_select_sink_always_included` (PASS) | zero scores (all equal), n_sink=3 | **verified (synthetic)** | Sinks are added unconditionally before dynamic selection. |
| 65 | High-score tokens preferentially selected | docs | `quantizers/snapkv.py` `snap_select_indices` | `::test_select_high_score_tokens_preferred` (PASS) | planted scores 0.90/0.85 at positions 3/4 | **verified (synthetic)** | Core claim: the selection is score-ranked, not positional. |
| 66 | snapkv_compress output shape [min(budget,S), D], dtype fp16 | docs | `quantizers/snapkv.py` `snapkv_compress` | `::test_compress_output_shape`, `::test_compress_output_dtype_fp16`, `::test_compress_n_kept_matches_budget` (PASS) | S=64, budget=16, D=128 | **verified (synthetic)** | State.kept_keys and kept_values both [n_kept, D] fp16. |
| 67 | budget >= S: all tokens kept, no eviction | docs | `quantizers/snapkv.py` `snapkv_compress` | `::test_compress_no_eviction_short_seq` (PASS) | S=10, budget=50 | **verified (synthetic)** | Keep-all edge case — n_kept == S == 10. |
| 68 | eviction_ratio > 1 after prefill with budget < S | docs | `cache/snapkv_cache.py` `eviction_ratio` | `tests/cache/test_snapkv_cache.py::test_byte_ordering_eviction_ratio_gt_1` (PASS) | S=64, budget=16 | **verified (synthetic)** | full / kept = 4× in this case (64/16). |
| 69 | keep_rate in (0, 1] after realistic-budget prefill | docs | `cache/snapkv_cache.py` `keep_rate` | `::test_keep_rate_in_range` (PASS) | S=64, budget=16, n_sink=2 | **verified (synthetic)** | 16/64 = 0.25. Budget >= S gives keep_rate == 1.0. |
| 70 | Decode accumulation: seq dim grows by 1 per single-token call | docs | `cache/snapkv_cache.py` `update_and_fetch` | `::test_decode_accumulation` (PASS) | prefill S=64 then 4 decode steps | **verified (synthetic)** | After prefill: 16 kept; after 4 decode: 20. |
| 71 | Decode-only first call (S=1) has keep_rate == 1.0 (no eviction) | docs | `cache/snapkv_cache.py` `_process_decode` | `::test_decode_only_no_eviction` (PASS) | S=1 first call | **verified (synthetic)** | S==1 → is_prefill=False → _process_decode, no eviction. |
| 72 | Determinism; for_model propagates snap_budget, snap_obs_window, snap_n_sink | docs | `cache/snapkv_cache.py`, `cache/base.py` `KVCacheBuilder.for_model` | `::test_deterministic`, `::test_build_via_for_model_propagates_config` (PASS) | same K/V → same output; fake 2-layer model | **verified (synthetic)** | for_model uses dataclasses.replace — no builder branch needed. |
| 73 | SnapKV "adapted", not faithful port; proxy limitation disclosed | all docs | `snapkv_cache.py` / `snapkv.py` docstrings | design decision: key-as-query proxy, no max-pool smoothing, uniform budget | — | **verified (by construction)** | Labeling rule: "SnapKV-adapted (key-as-query proxy)". Proxy limitation in docs, CHANGELOG, README. |
| 74 | SnapKV end-to-end throughput/eviction | (none claimed) | `benchmark_scripts/benchmark_snapkv.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — no results_snapkv.json committed | — | **unverified — explicitly not claimed** | Offline harness measures attention-coverage lift vs random baseline; not model-level. |


## StreamingLLM-adapted structural eviction (0.20.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 75 | init_streaming_window creates empty buffers with correct shapes [0, D] | docs | `quantizers/streaming_llm.py` `init_streaming_window` | `tests/quantizers/test_streaming_llm.py::test_init_window_empty`, `::test_init_window_shapes` (PASS) | D=64 | **verified (synthetic)** | Empty NamedTuple with (0, D) sink/recent arrays. |
| 76 | First n_sink tokens absorb into sink buffer; remaining go to recent window | docs | `quantizers/streaming_llm.py` `stream_update` | `::test_sink_absorption_single_step`, `::test_sink_absorption_partial` (PASS) | n_sink=4, S=6 | **verified (synthetic)** | Token-by-token loop branches on current_sinks < n_sink. |
| 77 | Sink buffer frozen after fill — additional tokens do not extend sinks | docs | `quantizers/streaming_llm.py` `stream_update` | `::test_sinks_are_frozen_after_fill` (PASS) | n_sink=4, two 4-token calls | **verified (synthetic)** | After first call: n_sink=4; second call: n_sink still 4. |
| 78 | tokens_seen accumulates correctly across multiple calls | docs | `quantizers/streaming_llm.py` `StreamingWindow.tokens_seen` | `::test_tokens_seen_accumulates` (PASS) | two calls: S=6, S=3 | **verified (synthetic)** | tokens_seen_before + S tracked per update call. |
| 79 | Recent window trims to last window_size tokens when exceeded | docs | `quantizers/streaming_llm.py` `stream_update` | `::test_window_trim_at_capacity` (PASS) | n_sink=2, window_size=4, 8 recent tokens | **verified (synthetic)** | Trim: recent_k[-window_size:] when len > window_size. |
| 80 | n_recent <= window_size across 20 single-token decode steps | docs | `quantizers/streaming_llm.py` `stream_update` | `::test_window_never_exceeds_capacity_multi_step` (PASS) | n_sink=2, window_size=5, 20 steps | **verified (synthetic)** | Checked at every step. |
| 81 | n_sink + n_recent <= n_sink + window_size across 30-step stress test | docs | `quantizers/streaming_llm.py` `StreamingWindow` | `::test_total_in_window_bounded` (PASS) | n_sink=4, window_size=8, 30 single-token steps | **verified (synthetic)** | Constant-memory guarantee from the primitives layer. |
| 82 | stream_get_kv returns [n_sink + n_recent, D] fp16 with sinks first | docs | `quantizers/streaming_llm.py` `stream_get_kv` | `::test_get_kv_combined_shape`, `::test_get_kv_dtype_fp16`, `::test_get_kv_sinks_first` (PASS) | n_sink=2, window_size=4, known values | **verified (synthetic)** | Sink rows at value 1.0; recent rows at -1.0; first 2 rows of output are 1.0. |
| 83 | stream_fp16_bytes = (n_sink + n_recent) × D × 4 (K+V, fp16) | docs | `quantizers/streaming_llm.py` `stream_fp16_bytes` | `::test_stream_fp16_bytes_formula`, `::test_stream_fp16_bytes_after_trim` (PASS) | n=12 (4 sink+8 recent), D=64 | **verified (synthetic)** | Formula: n * D * 2 * 2. |
| 84 | n_sink=0 edge: all tokens go to recent window | docs | `quantizers/streaming_llm.py` `stream_update` | `::test_n_sink_zero` (PASS) | n_sink=0, S=8, window_size=4 | **verified (synthetic)** | current_sinks < n_sink (0<0) is always False; all tokens → recent. |
| 85 | StreamingLLMKVCache factory dispatch and no .bits attribute | docs | `cache/streaming_llm_cache.py`, `cache/base.py` | `tests/cache/test_streaming_llm_cache.py::test_factory_dispatch`, `::test_no_bits_attribute` (PASS) | method="streaming_llm" | **verified (synthetic)** | isinstance check; hasattr(c, 'bits') is False; hasattr(c, 'streaming_ratio') is True. |
| 86 | Output shape bounded by n_sink + window_size after large prefill | docs | `cache/streaming_llm_cache.py` `update_and_fetch` | `::test_output_shape_bounded_by_sink_plus_window`, `::test_large_prefill_trimmed_correctly` (PASS) | S=32/1000, n_sink=4, window=8 | **verified (synthetic)** | shape[2] == 12 for S=1000 (4+8 trim). |
| 87 | streaming_ratio == 1.0 when all tokens fit; > 1.0 after overflow | docs | `cache/streaming_llm_cache.py` `streaming_ratio` | `::test_streaming_ratio_equals_1_before_window_fills`, `::test_streaming_ratio_gt_1_after_overflow` (PASS) | window_size=100 (no overflow) vs S=100, window=8 (overflow) | **verified (synthetic)** | Ratio = full_seq_bytes / stream_kept_bytes. |
| 88 | Window trims oldest recent tokens (not newest) when FIFO overflows | docs | `cache/streaming_llm_cache.py` `update_and_fetch` | `::test_window_trims_oldest_recent` (PASS) | n_sink=2, window_size=4, 8 decode steps | **verified (synthetic)** | FIFO semantics: trim front (oldest). Final seq dim == 6. |
| 89 | Determinism; for_model propagates stream_n_sink, stream_window_size | docs | `cache/streaming_llm_cache.py`, `cache/base.py` `KVCacheBuilder.for_model` | `::test_deterministic`, `::test_build_via_for_model_propagates_config` (PASS) | same K/V → same output; fake 2-layer model | **verified (synthetic)** | for_model uses dataclasses.replace — no builder branch needed. |
| 90 | StreamingLLM "adapted", not faithful port; limitations disclosed | all docs | `streaming_llm_cache.py` / `streaming_llm.py` docstrings | design decision: no mask adjustment, no position-ID remapping, fixed sink count | — | **verified (by construction)** | Labeling rule: "StreamingLLM-adapted (VeloxQuant-MLX implementation)". |
| 91 | StreamingLLM end-to-end throughput/eviction | (none claimed) | `benchmark_scripts/benchmark_streaming_llm.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — no results_streaming_llm.json committed | — | **unverified — explicitly not claimed** | Offline harness measures streaming_ratio across (seq_len, window_size) sweep; not model-level. |

## xKV-adapted cross-layer shared-subspace compression (0.27.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 92 | pair_layers_grouped assigns contiguous groups; trailing partial group is a smaller valid group | docs | `quantizers/xkv.py` `pair_layers_grouped` | `tests/quantizers/test_xkv.py::test_pair_layers_grouped_contiguous_pairs`, `::test_pair_layers_grouped_trailing_partial_group`, `::test_pair_layers_grouped_size_one_all_degenerate` (PASS) | n_layers=4/5/3, group_size=2/2/1 | **verified (synthetic)** | Trailing group of 5 layers at group_size=2 is (0,2,1) — size 1, not padded. |
| 93 | joint_svd_compress on a single matrix (N=1) matches svd_compress_keys (SVDq) at the same rank | docs | `quantizers/xkv.py` `joint_svd_compress` | `tests/quantizers/test_xkv.py::test_joint_svd_group_of_one_matches_plain_svd` (PASS) | D=32, S=64, rank=8 | **verified (synthetic)** | K_mean and |singular values| match within 1e-3/1e-4 atol; group-of-1 degeneracy. |
| 94 | Shared basis fit jointly across layers with genuinely shared low-rank structure reconstructs better than independent SVD on unrelated noise at matched rank | docs | `quantizers/xkv.py` | `tests/quantizers/test_xkv.py::test_joint_svd_shared_structure_helps_reconstruction` (PASS) | D=32, r_true=4, S=64, 3 synthetic layers | **verified (synthetic)** | Mechanism validation, not just plumbing — shared-structure MSE < independent-on-noise MSE. |
| 95 | project_into_shared_basis + reconstruct_from_shared_basis round-trip recovers input without quantization noise | docs | `quantizers/xkv.py` | `tests/quantizers/test_xkv.py::test_round_trip_recovers_without_quantization` (PASS) | full rank (r=D=16) | **verified (synthetic)** | float32 precision floor (atol=1e-2). |
| 96 | All members of a group receive the identical shared basis after a settle round | docs | `cache/xkv_cache.py`, `cache/xkv_coordinator.py` | `tests/cache/test_xkv_cache.py::test_all_members_receive_identical_basis` (PASS) | 3-member group, D=32, rank=8 | **verified (synthetic)** | Requires a settle round: only the last-to-publish member adopts immediately; earlier members adopt on their next call. |
| 97 | Only member_idx==0 (leader) reports nonzero shared_basis_bytes; followers report 0 | docs | `cache/xkv_cache.py` `_account_basis_bytes` | `tests/cache/test_xkv_cache.py::test_only_leader_reports_basis_bytes` (PASS) | 3-member group | **verified (synthetic)** | Avoids double-counting the amortized basis cost when bytes are summed across layers. |
| 98 | compressed_key_bytes < fp16_key_bytes | docs | `cache/xkv_cache.py` | `tests/cache/test_xkv_cache.py::test_byte_accounting_compressed_less_than_fp16` (PASS) | S=64, rank=8 | **verified (synthetic)** | |
| 99 | Decode-time calls after prefill project into the frozen basis without re-triggering the joint SVD | docs | `cache/xkv_cache.py` `update_and_fetch` | `tests/cache/test_xkv_cache.py::test_decode_accumulation_uses_frozen_basis` (PASS) | prefill S=32 + settle + 4 decode steps | **verified (synthetic)** | V_g array identical before/after decode steps (exact equality, not just close). |
| 100 | Coordinator max_ctx guard raises on prefill overflow | docs | `cache/xkv_coordinator.py` `publish_member_keys` | `tests/cache/test_xkv_cache.py::test_coordinator_budget_raises` (PASS) | max_ctx=8, prefill S=16 | **verified (synthetic)** | Guard fires on member_idx==0's publish only (the only member incrementing published_tokens); decode-time calls after adoption never re-publish. |
| 101 | for_model builds correct member/group assignment across layers, including a trailing partial group; one shared coordinator instance | docs | `cache/base.py` `KVCacheBuilder._build_xkv` | `tests/cache/test_xkv_cache.py::test_for_model_grouping`, `::test_for_model_trailing_partial_group` (PASS) | 6-layer and 5-layer fake models, group_size=2 | **verified (synthetic)** | id() check confirms all layers share one XKVCoordinator instance. |
| 102 | Determinism | docs | `cache/xkv_cache.py` | `tests/cache/test_xkv_cache.py::test_determinism` (PASS) | same K/V → same output across two independent runs | **verified (synthetic)** | |
| 103 | xKV "adapted", not faithful port; grouping/reconstruction simplifications disclosed | all docs | `xkv_cache.py` / `xkv.py` / `xkv_coordinator.py` docstrings | design decision: fixed contiguous grouping (no CKA validation), no Selective Reconstruction, single-bit-width latent quant (not mixed-bit), keys only | — | **verified (by construction)** | Labeling rule: "xKV-adapted (VeloxQuant-MLX implementation)". |
| 104 | xKV end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_xkv.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — `xkv_benchmark_results.json` is offline-synthetic only | — | **unverified — explicitly not claimed** | Offline harness measures reconstruction MSE parity (within ~1% of independent SVD) and byte savings (8–20% fewer bytes than independent SVD, improving with group size) against an independent-per-layer-SVD baseline; not model-level perplexity/throughput. |

## NSNQuant-adapted calibration-free universal-codebook VQ (0.28.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 105 | NSN round-trip (no VQ) is exact to fp16 metadata precision; post-NSN tokens have norm sqrt(d) and ~zero channel mean | docs | `quantizers/nsnquant.py` `nsn_transform`/`nsn_inverse` | `tests/quantizers/test_nsnquant.py::test_nsn_roundtrip_exact_without_vq`, `::test_nsn_output_statistics` (PASS) | (2,4,37,128) and (1,2,256,128) synthetic | **verified (synthetic)** | Restoration formula x̂ = s1·(s2·x_nsn + o); second Normalize's mean perturbation asserted small (paper: "negligible"). |
| 106 | Universal codebook is deterministic per seed, unit-norm, model/data-independent (synthetic Gaussian only); magnitude variant positive-orthant | docs | `quantizers/nsnquant.py` `build_universal_codebook` | `tests/quantizers/test_nsnquant.py::test_codebook_deterministic`, `::test_codebook_shape_and_norms` (PASS) | 256×8, seeded spherical k-means | **verified (synthetic)** | Never persisted, never fit on activations — the calibration-free property is by construction. |
| 107 | 2-bit / 1-bit VQ round-trip cosine floors on Gaussian input; 2-bit strictly beats 1-bit | docs | `quantizers/nsnquant.py` `vq_encode`/`vq_decode` | `tests/quantizers/test_nsnquant.py::test_vq_2bit_gaussian_cosine_floor` (>0.92), `::test_vq_1bit_gaussian_cosine_floor` (>0.78), `::test_vq_2bit_beats_1bit` (PASS) | (1,2,256,128) Gaussian | **verified (synthetic)** | Floors calibrated once then pinned (measured ~0.94 / ~0.82). |
| 108 | Mechanism validation: full NSN pipeline beats identical Hadamard+VQ without NSN on channel-biased input by a pinned margin | docs | `quantizers/nsnquant.py` | `tests/quantizers/test_nsnquant.py::test_nsn_beats_no_nsn_on_channel_biased_input` (PASS, margin >0.02) | bias·N(0,1) channel mean, outlier channels, token scale spread | **verified (synthetic)** | Benchmark sweep: +0.038 (2b) / +0.110 (1b) at bias=4.0, collapsing to ~+0.001–0.002 at bias=0 — NSN only helps when there is a bias to remove. |
| 109 | Prefill vs token-by-token decode yield identical quantized state; decode tokens are quantized once aged past the chunk frontier | docs | `cache/nsnquant_cache.py` `update_and_fetch` | `tests/cache/test_nsnquant_cache.py::test_prefill_decode_path_independence`, `::test_decode_accumulation_across_flushes` (PASS) | r=8, S=29 and 3r decode steps | **verified (synthetic)** | Chunk boundaries identical by construction; unlike KIVI's incoming-block-only simplification. |
| 110 | Byte accounting matches closed form (payload nsn_bits bits/elem + fp16 s1/s2 per token + fp16 o per chunk); >4× vs fp16 at T≫r; ~2.5 eff. bits at 2-bit defaults | docs | `cache/nsnquant_cache.py` `_account_chunk_bytes` | `tests/cache/test_nsnquant_cache.py::test_byte_accounting_closed_form`, `::test_compression_ratio_beats_fp16_at_long_context`, `::test_2bit_payload_double_of_1bit` (PASS) | B=1,H=2,D=128,r=16 | **verified (synthetic)** | Metadata counted, not waved away (paper double-quantizes to ~0.23 bits; we report ~0.5 fp16). |
| 111 | NSNQuant "adapted", not faithful port; simplifications disclosed | all docs | `nsnquant.py` / `nsnquant_cache.py` docstrings | design decision: post-RoPE keys, explicit value Hadamard, k-means-only codebook (no gradient fine-tune), fp16 metadata, no fused kernels | — | **verified (by construction)** | Labeling rule: "NSNQuant-adapted (VeloxQuant-MLX implementation)". |
| 112 | NSNQuant end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_nsn.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — `nsn_benchmark_results.json` is offline-synthetic only | — | **unverified — explicitly not claimed** | Harness measures reconstruction cosine/MSE (NSN ablation + KIVI-2bit baseline) and bytes/token breakdown; 0.96–0.98 cosine at ~2.5 eff. bits (2-bit), KIVI-2bit 0.66–0.88 on same inputs; not model-level perplexity/throughput. |

## L2Norm-adapted intrinsic key-norm eviction (0.29.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 113 | Over budget, the kept set equals the budget lowest-norm positions in original temporal order; sinks/recent windows protected; guards raise at build time | docs | `quantizers/knorm.py` `knorm_update` | `tests/quantizers/test_knorm.py::test_over_budget_keeps_lowest_norms_in_order`, `::test_sinks_protected_even_with_highest_norms`, `::test_recent_window_protected`, `::test_guard_sink_plus_recent_vs_budget` (PASS) | S=24-32, D=8, budget 8-12 | **verified (synthetic)** | Kept indices checked against manual numpy argsort. |
| 114 | Norms are intrinsic — computed once at insertion, never updated | docs | `quantizers/knorm.py` | `tests/quantizers/test_knorm.py::test_norms_immutable_across_updates` (PASS) | two sequential updates | **verified (synthetic)** | The property that enables both the speed and path-independence claims. |
| 115 | Path independence at recent=0: block vs token-by-token arrival yield bit-for-bit identical kept keys AND values | docs | `quantizers/knorm.py`, `cache/knorm_cache.py` | `tests/quantizers/test_knorm.py::test_path_independence_block_vs_tokenwise`, `tests/cache/test_knorm_cache.py::test_prefill_decode_bit_for_bit_equivalence` (PASS) | S=40/50, budget 10/12, n_sink=2 | **verified (synthetic)** | "Keep k best with a heap" invariant; breaks with recent>0 (documented). |
| 116 | keep-low preserves probe attention output better than keep-high under paper-like geometry | docs | `cache/knorm_cache.py` | `tests/cache/test_knorm_cache.py::test_keep_low_beats_keep_high_under_paper_geometry` (PASS) | constructed geometry: low-norm keys aligned to query cluster, high-norm anti-aligned | **verified (synthetic, constructed geometry)** | Validates machinery GIVEN the paper's geometry — the geometry itself is the paper's claim (arXiv:2406.11430), never claimed as independently verified. |
| 117 | ~100-800x faster than H2O-adapted at prefill (0.3-1.2 ms vs 37-275 ms) | docs, CHANGELOG | `quantizers/knorm.py` (vectorized top-k) | `benchmark_scripts/knorm_benchmark_results.json` `knorm_ms`/`h2o_ms` columns | S=256-1024, budget 64/128, M-series | **verified (synthetic)** | Consequence of no per-token softmax-over-cache loop. |
| 118 | Benchmark: keep-low beats random by +0.17 and keep-high by +0.21 mean perturbation under paper-like geometry; advantage REVERSES under isotropic control (~0.07 worse than random) | docs, CHANGELOG | `benchmark_scripts/benchmark_knorm.py` | committed `knorm_benchmark_results.json` | 12-row sweep, head_dim=64, n_sink=4, 32 probes | **verified (synthetic)** | Both directions reported; isotropic reversal is expected (softmax favors high-norm keys absent trained-LM geometry). |
| 119 | L2Norm "adapted", not faithful port; the low-norm=>high-attention correlation attributed to the paper, not validated here | all docs | `knorm.py` / `knorm_cache.py` docstrings | design decision: uniform budget, no RoPE remapping, recent/keep-high extensions off by default | — | **verified (by construction)** | Labeling rule: "L2Norm-adapted (VeloxQuant-MLX implementation)". |
| 120 | L2Norm end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_knorm.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only | — | **unverified — explicitly not claimed** | Output-perturbation and byte-accounting numbers only. |

## SKVQ-adapted sliding-window reorder + clip quantization (0.30.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 121 | alpha=1 clip window is exactly plain asymmetric min/max group quantization | docs | `quantizers/skvq.py` `clipped_group_quant` | `tests/quantizers/test_skvq.py::test_alpha1_equals_plain_minmax` (PASS) | vs manual numpy reference, 2-bit, gs=8 | **verified (synthetic)** | >=99.9% identical codes, recon within one code step (1-ulp float boundary tolerance). |
| 122 | Per-group clip search is never worse than alpha=1 under its search metric, strictly better on outlier-heavy groups | docs, CHANGELOG | `quantizers/skvq.py` (alpha grid contains 1.0) | `tests/quantizers/test_skvq.py::test_clip_search_never_worse_than_alpha1` (PASS) | sparse 20x outliers, 2-bit | **verified (synthetic)** | Guaranteed by construction (argmin over a grid containing the identity). |
| 123 | Reordering reduces round-trip MSE under smooth channel heterogeneity and buys ~nothing on homogeneous channels; collapses per-channel normalized error when outlier channels fill whole groups | docs, CHANGELOG | `quantizers/skvq.py` `channel_permutation` | `tests/quantizers/test_skvq.py::test_reorder_helps_heterogeneous_not_homogeneous`, `::test_reorder_large_win_on_per_channel_snr` (PASS) | logspace / outlier-block scale constructions, 2-bit | **verified (synthetic, constructed regimes)** | Absolute-MSE win requires smooth heterogeneity; with few extreme outliers the win moves to the normalized metric (documented in both tests). |
| 124 | Prefill vs token-by-token decode produce bit-for-bit identical caches; permutations frozen from chunk 0; flushed chunks never change; sink rows fp16-exact | docs, CHANGELOG | `cache/skvq_cache.py` | `tests/cache/test_skvq_cache.py::test_prefill_decode_bit_for_bit_equivalence`, `::test_permutations_frozen_from_first_chunk`, `::test_chunks_frozen_after_flush`, `::test_sink_rows_fp16_exact_after_flush` (PASS) | S=53, window 16, heterogeneous channels | **verified (synthetic)** | Same chunk-flush path-independence argument as NSNQuant, plus frozen first-chunk statistics. |
| 125 | Byte accounting matches the closed form (codes + fp16 lo/scale per token-group + fp16 window/sinks + one-off int32 perms) | docs | `cache/skvq_cache.py`, `quantizers/skvq.py` byte helpers | `tests/cache/test_skvq_cache.py::test_byte_accounting_closed_form`, `tests/quantizers/test_skvq.py::test_byte_helpers` (PASS) | r=16, n_sink=4, D=64, gs=16 | **verified (analytic)** | Searched alpha adds zero storage (folded into lo/scale). |
| 126 | Benchmark: reordering cuts key MSE a further 16.9% on top of clip search (heterogeneous regime) and -0.3% (nothing) under the homogeneous control; clip search adds 14.0% on top of reordering; per-channel normalized error collapses ~450x; KIVI reference wins several heterogeneous rows | docs, CHANGELOG | `benchmark_scripts/benchmark_skvq.py` | committed `skvq_benchmark_results.json` | 8-row sweep, D=64, gs=16, window 128, sink 5, bits 2/4 | **verified (synthetic)** | Both regimes + KIVI's wins reported; KIVI's per-channel keys are intrinsically immune to channel heterogeneity, and its wrapper always holds a trailing fp16 block. |
| 127 | SKVQ end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_skvq.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only | — | **unverified — explicitly not claimed** | Reconstruction/perturbation and byte-accounting numbers only; heterogeneous-channel premise attributed to the papers (SKVQ/KIVI/KVQuant). |

## Q-Filters-adapted query-agnostic projection eviction (0.31.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 128 | The paper's query-derived filter is **NOT implemented**; the direction is estimated from the SVD of observed **keys**, frozen after `calib_tokens` | all docs, CHANGELOG "Honest scope" | `quantizers/qfilters.py` `estimate_filter_dir` | design decision (documented deviation) | — | **verified (by construction)** | The core adaptation: a cache never sees query vectors. A different estimator of the same head-geometry direction, never claimed equivalent. |
| 129 | The key-SVD estimator recovers the dominant **axis** but not which **end** is important, so `qfilters_sign` is a genuine ablation | docs, CHANGELOG | `quantizers/qfilters.py` (deterministic pivot sign) | `benchmark_scripts/qfilters_benchmark_results.json` (`filter_cosine ≈ 0.97` while the winning raw sign arm flips row to row) | 12-row sweep, D=64, calib=64 | **verified (synthetic)** | The sign a query would disambiguate is unavailable at the cache; this is the measured consequence of claim 128. |
| 130 | `estimate_filter_dir` recovers a planted dominant direction (cosine > 0.99) | docs | `quantizers/qfilters.py` | `tests/quantizers/test_qfilters.py::test_estimate_filter_dir_recovers_planted_axis` (PASS) | N=300, D=8, variance concentrated on one axis | **verified (synthetic)** | Confirms the axis-recovery half of claim 129. |
| 131 | Over budget, the kept set equals the budget highest-projection positions in original temporal order; sinks/recent protected; guards and invalid sign raise at build time | docs | `quantizers/qfilters.py` `qfilters_update` | `tests/quantizers/test_qfilters.py::test_over_budget_keeps_highest_projection_in_order`, `::test_sinks_protected_even_with_low_projection`, `::test_recent_window_protected`, `::test_guard_and_sign_validation` (PASS) | S=64, D=8, budget 20 | **verified (synthetic)** | Kept indices checked against manual numpy ranking on the frozen direction. |
| 132 | Frozen-filter determinism — once frozen, a stored token's score never changes; below `calib_tokens` nothing is evicted | docs | `quantizers/qfilters.py`, `cache/qfilters_cache.py` | `tests/quantizers/test_qfilters.py::test_filter_frozen_and_scores_stable`, `::test_pre_calibration_passthrough_even_over_budget`, `tests/cache/test_qfilters_cache.py::test_pre_calibration_passthrough` (PASS) | two sequential updates; 50 tokens < calib 100/200 | **verified (synthetic)** | The calibration gate: no eviction can happen before the filter exists. |
| 133 | Path-DEPENDENT (contrast with L2Norm): prefill vs decode may freeze different filters; only both-within-budget + valid-unit-filter asserted, NOT bit-for-bit equivalence | docs, CHANGELOG | `cache/qfilters_cache.py` | `tests/cache/test_qfilters_cache.py::test_prefill_decode_both_valid_not_equivalent` (PASS) | S=80, budget 20, n_sink=2 | **verified (synthetic)** | Deliberately no equivalence guarantee — the honest contrast with knorm's path-independence. |
| 134 | The correct-sign projection cache preserves probe attention output far better than random eviction under paper-like geometry | docs | `cache/qfilters_cache.py` | `tests/cache/test_qfilters_cache.py::test_projection_scorer_beats_random_under_paper_geometry` (PASS) | constructed geometry: important tokens high-projection + aligned with query cluster, S=256, budget 28 | **verified (synthetic, constructed geometry)** | Validates machinery GIVEN the paper's anisotropy — the anisotropy itself is the paper's claim (arXiv:2503.02812), never independently verified. Best-of-two-signs used because the estimator is sign-ambiguous (claim 129). |
| 135 | Benchmark: best-sign Q-Filter beats random by +0.16 mean perturbation under paper-like geometry; raw single-sign arms hover at random under the isotropic control | docs, CHANGELOG | `benchmark_scripts/benchmark_qfilters.py` | committed `qfilters_benchmark_results.json` | 12-row sweep, head_dim=64, n_sink=4, calib=64, 32 probes | **verified (synthetic)** | Isotropic residual is the best-of-two selection bonus, not an importance signal; reported in full. |
| 136 | Byte accounting matches the closed form (K+V fp16 for kept tokens + one float32 filter direction per head) | docs | `cache/qfilters_cache.py`, `quantizers/qfilters.py` byte helpers | `tests/cache/test_qfilters_cache.py::test_compression_ratio_math`, `tests/quantizers/test_qfilters.py::test_bytes_accounting_includes_filter` (PASS) | budget 8/16, D=16/64, 2 heads | **verified (analytic)** | Filter direction counted in full, same discipline as SKVQ permutation tables. |
| 137 | Q-Filters "adapted", not faithful port; the QK-anisotropy/attention-prediction claim attributed to the paper, not validated here | all docs | `qfilters.py` / `qfilters_cache.py` docstrings | design decision: key-SVD filter, uniform budget, no RoPE remapping, recent extension off | — | **verified (by construction)** | Labeling rule: "Q-Filters-adapted (VeloxQuant-MLX implementation)"; preprint, no venue asserted. |
| 138 | Q-Filters end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_qfilters.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only | — | **unverified — explicitly not claimed** | Output-perturbation and byte-accounting numbers only. |

## Keyformer-adapted Gumbel-regularized heavy-hitter eviction (0.32.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 139 | The paper's **annealed, redrawn** Gumbel schedule is **NOT implemented**; the noise is a **frozen deterministic per-position** Gumbel draw keyed by `(seed, position)` | all docs, CHANGELOG "Honest scope" | `quantizers/keyformer.py` `_gumbel_at` | design decision (documented deviation) | — | **verified (by construction)** | A cache has no trustworthy global step counter; the frozen draw preserves the "don't doom a borderline token on one low reading" intent, not the schedule. Never claimed equivalent. |
| 140 | `keyformer_tau=0` collapses onto H2O-adapted **bit-for-bit** over an identical stream | docs, CHANGELOG | `quantizers/keyformer.py`, `cache/keyformer_cache.py` | `tests/quantizers/test_keyformer.py::test_tau_zero_matches_h2o` (PASS); benchmark `h2o` cross-check column | 40-token stream, budget 12, n_sink 4, D=16 | **verified (synthetic)** | The only thing Keyformer adds over H2O is the Gumbel term; at tau=0 the eviction argmin is on the raw score, identical to H2O-adapted. |
| 141 | With no noise (tau=0) the kept set is **seed-invariant** at both quantizer and cache level | docs | `quantizers/keyformer.py`, `cache/keyformer_cache.py` | `tests/quantizers/test_keyformer.py::test_tau_zero_is_seed_invariant`, `tests/cache/test_keyformer_cache.py::test_tau_zero_seed_invariant_at_cache_level` (PASS) | 35–40 tokens, budget 10 | **verified (synthetic)** | Corollary of claim 140: the seed only feeds the Gumbel term, which tau=0 zeroes out. |
| 142 | The frozen Gumbel draw is deterministic per `(seed, position)`; a full run is reproducible | docs | `quantizers/keyformer.py` `_gumbel_at` | `tests/quantizers/test_keyformer.py::test_gumbel_deterministic_per_position`, `::test_run_is_reproducible` (PASS) | same/different seed & pos; tau=2, 30 tokens | **verified (synthetic)** | Inverse-CDF Gumbel over a seeded MLX key; same (seed,pos) → same value regardless of block chunking. |
| 143 | Budget is never exceeded (token-by-token or prefill block, across B/H shapes); sinks and `recent` window survive heavy eviction | docs | `quantizers/keyformer.py`, `cache/keyformer_cache.py` | `tests/quantizers/test_keyformer.py::test_budget_never_exceeded_token_by_token`, `::test_budget_never_exceeded_block_prefill`, `::test_sinks_survive_eviction`, `::test_recent_window_protects_trailing`, `tests/cache/test_keyformer_cache.py::test_budget_respected_decode`, `::test_budget_respected_prefill_block` (PASS) | S up to 60, budget 8–12, B=2 H=4 | **verified (synthetic)** | +inf protection on leading sinks and trailing recent positions in the eviction ranking. |
| 144 | Guards: negative `tau` and `n_sink + recent >= budget` raise at build time | docs | `quantizers/keyformer.py` `init_keyformer_state`, `cache/keyformer_cache.py` | `tests/quantizers/test_keyformer.py::test_init_rejects_negative_tau`, `::test_init_rejects_no_evictable_room`, `tests/cache/test_keyformer_cache.py::test_construction_guard_negative_tau`, `::test_construction_guard_no_evictable_room` (PASS) | — | **verified (synthetic)** | Cache delegates the guards to `init_keyformer_state` at construction. |
| 145 | Late-riser mechanism: a token that reads low early but aligns with a later burst survives at a **higher rate with the Gumbel term on than off** | docs, CHANGELOG | `quantizers/keyformer.py` | `tests/quantizers/test_keyformer.py::test_gumbel_rescues_late_riser` (PASS) | planted orthogonal-then-aligned token, 40 noise seeds, budget 10 | **verified (synthetic, statistical)** | A rate claim across seeds, not a per-seed guarantee — the honest form of the mechanism claim. |
| 146 | Benchmark: under late-riser geometry greedy tau=0 evicts the planted riser 100% of the time while tau=6 rescues it ~75%; downstream perturbation is a noisier regime-dependent secondary effect, reported as-is | docs, CHANGELOG | `benchmark_scripts/benchmark_keyformer.py` | committed `keyformer_benchmark_results.json` (deterministic in all non-timing fields) | 8-row sweep, D=32, n_sink=2, 5 noise seeds, tau∈{0,2,6} | **verified (synthetic)** | Survival rate is the clean mechanism observable; perturbation does NOT uniformly improve and is not cherry-picked. Stable-geometry control included. |
| 147 | Keyformer "adapted", not faithful port; key-as-query proxy for the base score; MLSys 2024 venue asserted | all docs | `keyformer.py` / `keyformer_cache.py` docstrings | design decision: frozen Gumbel, key-as-query proxy, uniform budget/tau, no RoPE remapping, recent extension off | — | **verified (by construction)** | Labeling rule: "Keyformer-adapted (VeloxQuant-MLX implementation)". |
| 148 | Keyformer end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_keyformer.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only | — | **unverified — explicitly not claimed** | Survival-rate, output-perturbation and byte-accounting numbers only. |

## MorphKV-adapted recent-window correlation retention (0.33.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 149 | `morphkv_window=1` collapses onto TOVA-adapted **bit-for-bit** over an identical stream | docs, CHANGELOG | `quantizers/morphkv.py`, `cache/morphkv_cache.py` | `tests/quantizers/test_morphkv.py::test_window_one_matches_tova` (PASS) | 40-token stream, budget 12, n_sink 4, D=16 | **verified (synthetic)** | With one recent key, recent-relevance is that key's attention over the keep set — exactly TOVA's latest-token argmin. |
| 150 | **No H2O collapse is claimed** — MorphKV recomputes retention from the live window each step, never accumulates | all docs, CHANGELOG "Honest scope" | `quantizers/morphkv.py` `morphkv_update` (no cumulative score array) | design decision (documented) | — | **verified (by construction)** | The only pinned reduction is window=1==TOVA; a wide window approaches averaging over recent context, never H2O's cumulative-forever rule. |
| 151 | A wider window is capable of retaining a **different** kept set than window=1 (the window axis is not vacuous) | docs | `quantizers/morphkv.py` | `tests/quantizers/test_morphkv.py::test_larger_window_can_differ_from_window_one` (PASS) | 60-token stream, budget 12, window 1 vs 8 | **verified (synthetic)** | Establishes that the recent-*window* mechanism does more than latest-token TOVA. |
| 152 | Constant-size budget never exceeded (token-by-token or prefill block, across B/H); sinks and trailing recent window survive heavy eviction | docs | `quantizers/morphkv.py`, `cache/morphkv_cache.py` | `tests/quantizers/test_morphkv.py::test_budget_never_exceeded_token_by_token`, `::test_budget_never_exceeded_block_prefill`, `::test_sinks_survive_eviction`, `::test_recent_window_protects_trailing`, `tests/cache/test_morphkv_cache.py::test_budget_respected_decode`, `::test_budget_respected_prefill_block` (PASS) | S up to 60, budget 8–12, B=2 H=4 | **verified (synthetic)** | +inf protection on leading sinks and the trailing `window` positions in the ranking. |
| 153 | Guards: bad budget/window and `n_sink + window >= budget` (and window > budget) raise at build time | docs | `quantizers/morphkv.py` `init_morphkv_state`, `cache/morphkv_cache.py` | `tests/quantizers/test_morphkv.py::test_init_rejects_bad_budget`, `::test_init_rejects_bad_window`, `::test_init_rejects_sink_ge_budget`, `::test_init_rejects_window_gt_budget`, `::test_init_rejects_no_evictable_room`, `tests/cache/test_morphkv_cache.py::test_construction_guard_bad_window`, `::test_construction_guard_no_evictable_room` (PASS) | — | **verified (synthetic)** | Cache delegates the guards to `init_morphkv_state` at construction. |
| 154 | MorphKV is deterministic — a full run is reproducible (no RNG) | docs | `quantizers/morphkv.py`, `cache/morphkv_cache.py` | `tests/quantizers/test_morphkv.py::test_run_is_reproducible`, `tests/cache/test_morphkv_cache.py::test_window_one_deterministic_at_cache_level` (PASS) | 30–35 tokens, budget 8–10 | **verified (synthetic)** | Retention recomputed from live keep set + window; no stochastic term. |
| 155 | Topic-shift mechanism: MorphKV retains the recent-relevant region at a **higher rate than a cumulative H2O-style baseline** | docs, CHANGELOG | `quantizers/morphkv.py` | `tests/quantizers/test_morphkv.py::test_topic_shift_retains_recent_relevant` (PASS); `::test_stable_control_no_advantage_required` (null control, PASS) | planted axis-A early / axis-B late, 30 data seeds, budget 10 | **verified (synthetic, statistical)** | A rate claim over seeds, not a per-seed guarantee — the honest form; stable control shows no advantage. |
| 156 | Benchmark: under topic_shift, cumulative H2O retains ~0% of the recent-relevant region while wider windows (8/32) retain ~0.39–0.74, beating the window=1 (TOVA) reference (~0.15–0.37); perturbation is a noisier secondary effect, reported as-is | docs, CHANGELOG | `benchmark_scripts/benchmark_morphkv.py` | committed `morphkv_benchmark_results.json` (deterministic in all non-timing fields) | 8-row sweep, D=32, n_sink=2, 5 data seeds, window∈{1,8,32} | **verified (synthetic)** | Recent-relevant retention is the clean mechanism observable; the recent signal is deliberately noisy so the window earns its keep. Stable-geometry control included. |
| 157 | MorphKV "adapted", not faithful port; key-as-query proxy; ICML 2025 venue asserted; paper's accuracy/memory numbers NOT reproduced | all docs | `morphkv.py` / `morphkv_cache.py` docstrings | design decision: recent-window proxy retention, uniform budget/window, no RoPE remapping | — | **verified (by construction)** | Labeling rule: "MorphKV-adapted (VeloxQuant-MLX implementation)"; the paper's 52.9% memory / 18.2% accuracy figures are the paper's, on trained models. |
| 158 | MorphKV end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_morphkv.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only | — | **unverified — explicitly not claimed** | Recent-relevant-retention, output-perturbation and byte-accounting numbers only. |

## KVzip-adapted context-reconstruction reliance retention (0.34.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 159 | `kvzip_probe="latest"` collapses onto TOVA-adapted **bit-for-bit** over an identical stream | docs, CHANGELOG | `quantizers/kvzip.py`, `cache/kvzip_cache.py` | `tests/quantizers/test_kvzip.py::test_latest_probe_matches_tova` (PASS) | 40-token stream, budget 12, n_sink 4, D=16 | **verified (synthetic)** | With a single-latest-key probe, reconstruction reliance is that key's attention over the keep set — exactly TOVA's latest-token argmin. |
| 160 | **No H2O collapse is claimed** — KVzip recomputes reconstruction reliance from the live keep set each step, never accumulates | all docs, CHANGELOG "Honest scope" | `quantizers/kvzip.py` `kvzip_update` (no cumulative score array) | design decision (documented) | — | **verified (by construction)** | The only pinned reduction is probe=latest==TOVA; the context probe is a max-over-keep-set reliance, never H2O's cumulative-forever rule. |
| 161 | The default `context` probe is capable of retaining a **different** kept set than `latest` (the reconstruction axis is not vacuous) | docs | `quantizers/kvzip.py` | `tests/quantizers/test_kvzip.py::test_context_probe_can_differ_from_latest` (PASS) | 60-token stream, budget 12, probe latest vs context | **verified (synthetic)** | Establishes that the reconstruction-reliance mechanism does more than latest-token TOVA. |
| 162 | Constant-size budget never exceeded (token-by-token or prefill block, across B/H, both probes); sinks survive heavy eviction | docs | `quantizers/kvzip.py`, `cache/kvzip_cache.py` | `tests/quantizers/test_kvzip.py::test_budget_never_exceeded_token_by_token`, `::test_budget_never_exceeded_block_prefill`, `::test_budget_respected_latest_probe`, `::test_sinks_survive_eviction`, `::test_sinks_survive_latest_probe`, `tests/cache/test_kvzip_cache.py::test_budget_respected_decode`, `::test_budget_respected_prefill_block` (PASS) | S up to 60, budget 8–12, B=2 H=4 | **verified (synthetic)** | +inf protection on leading sinks in the reconstruction-reliance ranking. |
| 163 | Guards: bad budget, `n_sink >= budget`, and an invalid `probe` raise at build time | docs | `quantizers/kvzip.py` `init_kvzip_state`, `cache/kvzip_cache.py` | `tests/quantizers/test_kvzip.py::test_init_rejects_bad_budget`, `::test_init_rejects_sink_ge_budget`, `::test_init_rejects_bad_probe`, `tests/cache/test_kvzip_cache.py::test_construction_guard_bad_probe`, `::test_construction_guard_sink_ge_budget` (PASS) | — | **verified (synthetic)** | Cache delegates the guards to `init_kvzip_state` at construction. |
| 164 | KVzip is deterministic — a full run is reproducible (no RNG) | docs | `quantizers/kvzip.py`, `cache/kvzip_cache.py` | `tests/quantizers/test_kvzip.py::test_run_is_reproducible`, `tests/cache/test_kvzip_cache.py::test_latest_probe_deterministic_at_cache_level` (PASS) | 30–35 tokens, budget 8–10 | **verified (synthetic)** | Reconstruction reliance recomputed from live keep set; no stochastic term. |
| 165 | Reconstruction-shift mechanism: KVzip retains the reconstruction-critical region at a **higher rate than a cumulative H2O-style baseline** | docs, CHANGELOG | `quantizers/kvzip.py` | `tests/quantizers/test_kvzip.py::test_reconstruction_geometry_retains_critical` (PASS); `::test_flat_control_no_advantage_required` (null control, PASS) | planted axis-A early / axis-B reconstruction-critical cluster, 30 data seeds, budget 10 | **verified (synthetic, statistical)** | A rate claim over seeds (measured gap 145 vs 0 axis-B rows), not a per-seed guarantee; flat control shows no advantage. |
| 166 | Benchmark: under reconstruction_shift, cumulative H2O retains ~0.017 of the reconstruction-critical region while the context probe retains ~0.609, beating the probe=latest (TOVA) reference (~0.248); perturbation is a noisier secondary effect, reported as-is | docs, CHANGELOG | `benchmark_scripts/benchmark_kvzip.py` | committed `kvzip_benchmark_results.json` (deterministic in all non-timing fields) | 8-row sweep, D=32, n_sink=2, 5 data seeds, probe∈{latest,context} | **verified (synthetic)** | Reconstruction-critical retention is the clean mechanism observable; the axis-B signal is deliberately noisy so the context probe earns its keep. Flat-geometry control included. |
| 167 | KVzip "adapted", not faithful port; key-as-reconstruction-probe proxy; NeurIPS 2025 Oral venue asserted; paper's numbers NOT reproduced | all docs | `kvzip.py` / `kvzip_cache.py` docstrings | design decision: reconstruction-reliance proxy retention, uniform budget/probe, no RoPE remapping, no head-level scoring | — | **verified (by construction)** | Labeling rule: "KVzip-adapted (VeloxQuant-MLX implementation)"; the paper's 3–4×/~2×/negligible-loss figures are the paper's, on trained models up to 170K tokens. |
| 168 | KVzip end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_kvzip.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only | — | **unverified — explicitly not claimed** | Reconstruction-critical-retention, output-perturbation and byte-accounting numbers only. |

## KVTC-adapted local PCA + DP-optimal bit allocation + entropy coding (0.35.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 169 | **Uniform-variance collapse**: with equal per-component variance and a contiguous `bit_choices` range, the DP-optimal allocation is exactly `floor(budget/n)` per component (remainder to the first components) | docs, CHANGELOG | `allocators/kvtc_dp.py` `dp_allocate_bits` | `tests/allocators/test_kvtc_dp.py::test_uniform_variance_collapses_to_floor_plus_remainder`, `::test_uniform_variance_collapse_matches_naive_uniform_splitter_helper` (PASS) | n∈{1,5,7}, budgets 0–56, contiguous bit_choices 0–8 | **verified (synthetic)** | The pinned reduction — the analogue of SVDq's fixed-split baseline / MorphKV's window=1==TOVA / KVzip's probe=latest==TOVA collapses. Falls out of the DP formulation via an infinitesimal index-order tie-break, not a special-cased branch. |
| 170 | The DP allocation matches a brute-force-optimal total distortion on small n, and no single-component bit reallocation lowers total distortion further (local optimality) | docs | `allocators/kvtc_dp.py` | `tests/allocators/test_kvtc_dp.py::test_matches_brute_force_optimum_small_n`, `::test_local_optimality_no_single_reallocation_improves` (PASS) | n=4–6, 5 seeds, exhaustive bit-choice search | **verified (synthetic)** | Confirms the DP is exact, not merely plausible — the honest crux is that the *objective* it exactly minimizes is an analytic proxy, not that the optimization itself is approximate. |
| 171 | Monotonicity: a strictly higher-variance component never receives fewer bits than a lower-variance one at the optimum | docs | `allocators/kvtc_dp.py` | `tests/allocators/test_kvtc_dp.py::test_monotonic_higher_variance_never_fewer_bits` (PASS) | n=8, 10 seeds, descending-sorted variances | **verified (synthetic)** | |
| 172 | A near-zero-variance component can be assigned exactly 0 bits under a tight budget (dropped entirely, no zero-filled placeholder stored) | docs | `allocators/kvtc_dp.py`, `quantizers/kvtc.py` | `tests/allocators/test_kvtc_dp.py::test_near_zero_variance_component_gets_zero_bits`, `tests/quantizers/test_kvtc.py::test_compress_zero_budget_drops_everything` (PASS) | planted near-zero-variance component, budget=4 | **verified (synthetic)** | The genuinely new axis vs Palu/SVDq's fixed floor and SpectralQuant's binary cutoff. |
| 173 | Order-0 Huffman entropy coder round-trips losslessly across alphabets/sizes, including degenerate single-symbol and empty inputs; the code table's own byte cost is counted, not hidden | docs, CHANGELOG "Honest scope" | `quantizers/_entropy_coding.py` | `tests/quantizers/test_entropy_coding.py` (15 tests, PASS) | n up to 2000, alphabets 2–256 | **verified (synthetic)** | Stdlib `heapq` only, no external dependency; static per-call table, not adaptive — stated plainly in the module docstring. |
| 174 | **The DP allocator beats SVDq's fixed top-25%/75% split at a matched total byte budget** on planted skewed-variance geometry — a rate over several seeds | docs, CHANGELOG | `quantizers/kvtc.py` `kvtc_compress` | `tests/quantizers/test_kvtc.py::test_dp_beats_fixed_split_at_matched_budget_skewed_variance`, `::test_dp_beats_fixed_split_mean_mse_skewed_variance` (PASS) | S=128, D=32, r_true=6, budget=D*3, 10–12 data seeds | **verified (synthetic, statistical)** | The clean mechanism observable. Matched total-bit budget, not matched per-arm hyperparameters — SVDq's own hi_bit/lo_bit knobs are re-derived at the same total budget for a fair comparison. |
| 175 | On flat (near-isotropic) geometry the DP allocator does not dramatically beat the fixed split — a null-ish control | docs, CHANGELOG | `quantizers/kvtc.py` | `tests/quantizers/test_kvtc.py::test_flat_variance_dp_does_not_dramatically_beat_fixed_split` (PASS) | S=128, D=32, isotropic Gaussian | **verified (synthetic)** | The honest counterpart to KVzip's flat control / MorphKV's stable control — the win is regime-dependent, not a free lunch. |
| 176 | Local PCA reduces to near-identity reconstruction at a generous bit budget (cosine similarity > 0.999) | docs | `quantizers/kvtc.py` | `tests/quantizers/test_kvtc.py::test_generous_budget_near_identity_reconstruction` (PASS) | S=64, D=16, budget=16·D | **verified (synthetic)** | |
| 177 | Byte accounting: `kvtc_fp16_bytes` includes the realized entropy-coded payload + code table + projection V + mean; `kvtc_pre_entropy_bytes` excludes the entropy-coding gain (fixed-width packed size only) | docs | `quantizers/kvtc.py` | `tests/quantizers/test_kvtc.py::test_fp16_bytes_includes_payload_table_projection`, `::test_pre_entropy_bytes_excludes_entropy_gain`, `::test_byte_helpers_zero_when_nothing_survives` (PASS) | S=64, D=16, budget=32 and 0 | **verified (analytic)** | Table overhead and entropy-coded payload are never hidden — the point of the Phase-4 spec requirement. |
| 178 | Values are compressed too, not keys-only (contrast with SVDq); both K and V independently pass through `kvtc_compress`/`kvtc_decompress` in `KVTCKVCache`, mirroring Palu's scope | docs | `quantizers/kvtc.py`, `cache/kvtc_cache.py` | `tests/quantizers/test_kvtc.py::test_compresses_arbitrary_tensor_not_just_keys`, `tests/cache/test_kvtc_cache.py::test_compression_ratio_above_one_on_structured_long_sequence` (PASS) | S=40 (quantizer level); S=512, low-rank planted (cache level) | **verified (synthetic)** | |
| 179 | Local PCA basis (`V`, `mean`) and the DP-derived per-component bit allocation are fixed once at prefill and reused UNCHANGED for every subsequent decode step — **not path-dependent**, in contrast with the eviction family H2O/TOVA/MorphKV/KVzip | docs, CHANGELOG "Honest scope" | `cache/kvtc_cache.py` `KVTCKVCache` | `tests/cache/test_kvtc_cache.py::test_basis_and_allocation_frozen_after_prefill`, `::test_deterministic_same_call_pattern_same_output` (PASS) | S=20 prefill + 15 decode steps, D=16 | **verified (synthetic, by construction)** | The determinism/non-path-dependence pin required by the implementation spec, mirroring how the eviction family's path-DEPENDENCE is pinned in the opposite direction. |
| 180 | `compression_ratio > 1` on structured (planted low-rank) long-sequence synthetic data, for both K and V, via the factory/`for_model` path | docs | `cache/base.py`, `cache/kvtc_cache.py` | `tests/cache/test_kvtc_cache.py::test_compression_ratio_above_one_on_structured_long_sequence`, `::test_for_model_propagates_kvtc_fields`, `::test_factory_dispatch` (PASS) | S=512, D=32, r_true=4, budget=48 | **verified (synthetic)** | The end-to-end factory smoke test target. |
| 181 | KVTC "adapted", not faithful port; local per-sequence PCA (not the paper's pre-calibrated global basis); analytic distortion proxy (not a real-activation-fit rate-distortion model); order-0 Huffman coder (not the paper's possibly more sophisticated scheme); ICLR 2026 venue asserted; paper's numbers NOT reproduced | all docs | `kvtc_dp.py` / `kvtc.py` / `_entropy_coding.py` / `kvtc_cache.py` docstrings | design decision: local PCA, analytic distortion proxy reused from `ratequant.py`, order-0 static Huffman table, uniform bit_choices/beta across heads | — | **verified (by construction)** | Labeling rule: "KVTC-adapted (VeloxQuant-MLX implementation)"; the paper's up-to-20x/40x, under-1pp-accuracy-loss figures on LLaMA 3/Mistral NeMo/R1-Qwen2.5 1.5B–70B are the paper's, on trained models. |
| 182 | KVTC end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_kvtc.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only, deterministic in all non-`_ms` fields (verified by diffing two runs) | — | **unverified — explicitly not claimed** | Matched-budget-distortion (MSE/cosine) and entropy-coding-realized-gain numbers only. |

## CurDKV-adapted value-aware leverage-score eviction (0.36.0) — added rows

| # | Claim | Asserted in | Implementing code | Measuring artifact | Conditions | Confidence | Notes |
|---|---|---|---|---|---|---|---|
| 183 | Two tokens with **identical keys** but **different values** receive **different** CurDKV leverage scores — direct proof the mechanism is value-aware, unlike H2O's key-only attention-mass score | docs, CHANGELOG | `quantizers/curdkv.py` `_leverage_scores` | `tests/quantizers/test_curdkv.py::test_identical_keys_different_values_diverge` (PASS) | D=16, shared key, one large-magnitude value vs one near-zero value | **verified (synthetic)** | The single, always-true, construction-level claim the whole method rests on. |
| 184 | Planted two-class geometry (near-identical keys, sharply divergent values): CurDKV retains value-relevant (class-1) tokens preferentially over value-irrelevant (class-2) tokens at a tight budget | docs, CHANGELOG | `quantizers/curdkv.py` `curdkv_update` | `tests/quantizers/test_curdkv.py::test_planted_geometry_curdkv_prefers_value_relevant_tokens` (PASS) | D=24, budget=6, 8 seeds, interleaved arrival order | **verified (synthetic, statistical)** | 8/8 trials retain all budget slots with class-1 tokens — a rate over seeds, not a single lucky run. Interleaved (not block-concatenated) arrival order is required to avoid confounding "value" with "recency" under the shared recency-sensitive eviction loop. |
| 185 | On the SAME planted geometry, H2O's key-only attention-mass score **cannot** distinguish the two classes and evicts near-uniformly — the baseline actually has the blind spot CurDKV fixes | docs, CHANGELOG | `quantizers/h2o.py` `h2o_update` (unmodified, imported for comparison) | `tests/quantizers/test_curdkv.py::test_h2o_blind_spot_on_same_planted_geometry` (PASS) | Same geometry/seed as row 184 | **verified (synthetic)** | Demonstrates the baseline's blind spot directly, rather than only asserting CurDKV is good in isolation. |
| 186 | Sink protection overrides leverage score: a sink-protected token with deliberately negligible value contribution still survives heavy eviction pressure | docs | `quantizers/curdkv.py` `curdkv_update` | `tests/quantizers/test_curdkv.py::test_sinks_never_evicted` (PASS) | D=8, n_sink=3, all-zero-value sink tokens, 20 subsequent updates | **verified (synthetic)** | Confirms the `+inf` sink-protection convention (shared with H2O) is applied before the leverage-based `argmin`, not after. |
| 187 | Degenerate all-zero-value block produces finite (non-NaN/inf) leverage scores, not a numerical fault from the SVD path | docs | `quantizers/curdkv.py` `_leverage_scores` | `tests/quantizers/test_curdkv.py::test_degenerate_all_zero_values_no_nan` (PASS) | D=16, all-zero value block | **verified (synthetic)** | Guarded by the early `if not np.any(wv_np): return zeros` branch. |
| 188 | Energy-weighted leverage-score estimator (not a hard top-k/bottom-(n−k) split) is required — a hard rank cutoff degenerates to uniform leverage (every row gets equal score) whenever the retained rank reaches the block size, regardless of true value magnitude | docs, CHANGELOG "Honest scope" | `quantizers/curdkv.py` `_leverage_scores` | Design/regression finding during implementation — confirmed by direct construction (2-row block, k=n=2, hard-cutoff `U` is a 2×2 orthogonal matrix, every row norm == 1 regardless of the actual singular values) | n=2, rank_cap=16 (k forced to n) | **verified (by construction)** | The reason the final estimator uses `l_i = Σⱼ (sⱼ²/Σs²)·U[i,j]²` (energy-weighted) rather than a hard top-k/bottom cutoff — documented as a load-bearing design decision, not an incidental implementation detail. |
| 189 | New tokens seeded with their own leverage score within the resulting (existing + new) block, not a flat 0, is required — a flat-0 seed lets a negligible-value newcomer tie forever with an already-negligible survivor, and index-based tie-breaking then resolves by arrival order/index rather than value | docs, CHANGELOG "Honest scope" | `quantizers/curdkv.py` `curdkv_update` | Design/regression finding during implementation — confirmed by direct construction (flat-0 seed reproduces a persistent 50/50 class split regardless of true leverage separation; self-leverage seed reproduces the expected 8/8 class-1-preferred outcome in row 184) | Planted two-class geometry, budget=6 | **verified (by construction)** | Distinguishes CurDKV's tie-break requirement from H2O's: H2O's softmax scores are never exactly 0 in practice, so this failure mode does not arise there; CurDKV's leverage scores legitimately can be exactly 0. |
| 190 | Budget never exceeded (bootstrap, multi-token, exact boundary, 30-step stress); `leverage_scores` array length always matches `keys` length | docs | `quantizers/curdkv.py` | `tests/quantizers/test_curdkv.py::test_budget_never_exceeded`, `::test_budget_exactly_enforced_at_boundary`, `::test_score_array_length_matches_keys`, `::test_30_step_stress_budget_constant` (PASS) | D=16–32, budgets 5–10 | **verified (synthetic)** | |
| 191 | Determinism — no RNG in the leverage-score path itself; same input sequence produces the same state and kept tokens | docs | `quantizers/curdkv.py`, `cache/curdkv_cache.py` | `tests/quantizers/test_curdkv.py::test_deterministic_across_identical_inputs`, `tests/cache/test_curdkv_cache.py::test_deterministic` (PASS) | D=32, budget=8 | **verified (synthetic)** | |
| 192 | Byte accounting: `curdkv_fp16_bytes` = `n_kept * D * 4` (K + V, fp16); `full_curdkv_fp16_bytes` formula; `compression_ratio` == 1 below budget, > 1 after evictions | docs | `quantizers/curdkv.py`, `cache/curdkv_cache.py` | `tests/quantizers/test_curdkv.py::test_curdkv_fp16_bytes_formula`, `::test_curdkv_fp16_bytes_empty_state`, `::test_full_curdkv_fp16_bytes_formula`, `tests/cache/test_curdkv_cache.py::test_compression_ratio_equals_1_below_budget`, `::test_compression_ratio_gt_1_after_evictions` (PASS) | D=64, budget=16 (quantizer level); D=32, budget=8/32 (cache level) | **verified (analytic)** | |
| 193 | Factory dispatch (`method="curdkv"` → `CurDKVKVCache`), `for_model` config propagation (`curdkv_budget`/`curdkv_n_sink`/`curdkv_rank_cap`), multi-head/multi-batch shape correctness, prefill and decode through the same eviction loop (no prefill-only special case, unlike SnapKV) | docs | `cache/base.py`, `cache/curdkv_cache.py` | `tests/cache/test_curdkv_cache.py::test_factory_dispatch`, `::test_build_via_for_model_propagates_config`, `::test_output_batch_head_dims_preserved`, `::test_prefill_then_decode_same_loop`, `::test_factory_smoke_compression_ratio_positive_both_kv` (PASS) | D=32, budget=6–64 | **verified (synthetic)** | The end-to-end factory smoke test target, exercising both K and V through the full `for_model` path. |
| 194 | CurDKV "adapted", not faithful port; key-as-query proxy (same as H2O/SnapKV); SVD-based, energy-weighted leverage-score estimator (not the paper's own CUR sampling algorithm); NeurIPS 2025 venue asserted; paper's numbers NOT reproduced | all docs | `curdkv.py` / `curdkv_cache.py` docstrings | design decision: key-as-query proxy, energy-weighted leverage-score estimator over the proxy attention-weighted value block, uniform budget/n_sink/rank_cap across heads, no RoPE remapping | — | **verified (by construction)** | Labeling rule: "CurDKV-adapted (VeloxQuant-MLX implementation)"; the paper's up-to-9.6%-higher-accuracy and up-to-40%-latency-reduction figures are the paper's, on trained models. |
| 195 | CurDKV end-to-end throughput/perplexity | (none claimed) | `benchmark_scripts/benchmark_curdkv.py` (offline-synthetic, loads no model) | **NOT RUN on hardware** — results JSON is offline-synthetic only, deterministic in all non-`_ms` fields (verified by diffing two runs) | — | **unverified — explicitly not claimed** | Class-2 (value-irrelevant) retention-rate numbers only; benchmark honestly reports that CurDKV retains fewer class-2 tokens than H2O on BOTH geometries tested (not only the planted_value_divergence design target), attributed to H2O's own small-N tie-break dynamics rather than overclaimed as general CurDKV dominance. |
